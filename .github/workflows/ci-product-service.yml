name: CI - product-service

on:
  push:
    branches: [develop]
    paths:
      - 'services/product-service/**'
  pull_request:
    branches: [develop]
    paths:
      - 'services/product-service/**'
  workflow_dispatch:

jobs: {}
