spring:
  application:
    name: order-service
  profiles:
    default: local

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus

services:
  http:
    connect-timeout: ${SERVICE_CONNECT_TIMEOUT:2s}
    read-timeout: ${SERVICE_READ_TIMEOUT:5s}
  storage:
    refund-attachment-bucket: ${REFUND_ATTACHMENT_BUCKET:refund-attachments}
